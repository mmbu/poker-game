# Poker Game - Quick Start Guide

## 🎯 Быстрый старт за 5 минут

### 1️⃣ Установка всех зависимостей

```bash
cd poker-game
npm install-all
```

### 2️⃣ Запуск локального Ethereum узла (терминал 1)

```bash
cd contracts
npm run hardhat node
```

Вы должны увидеть 20 тестовых аккаунтов с ETH. Скопируйте один приватный ключ и добавьте в `.env` файл бекенда.

### 3️⃣ Развертывание смарт-контракта (терминал 2)

```bash
cd contracts
npm run deploy
```

Скопируйте адрес развернутого контракта и добавьте его в `.env` бекенда.

### 4️⃣ Запуск бекенда (терминал 3)

```bash
cd backend
npm start
```

Вы должны увидеть:
```
Poker game server running on port 8080
WebSocket server running on port 8081
```

### 5️⃣ Запуск фронтенда (терминал 4)

```bash
cd frontend
npm run dev
```

Откройте браузер: **http://localhost:3000**

## 🎮 Первая игра

1. Создайте лобби
   - Нажмите "Create New Lobby"
   - Выберите размер стола (например, 6 игроков)
   - Установите blind 1-100

2. Создайте стол
   - Присоединитесь к лобби
   - Нажмите кнопку "Create New Table"

3. Присоединитесь к игре
   - Введите ваше имя
   - Выберите стек (например, 1000)
   - Нажмите "Join Table"

4. Начните игру
   - Нажмите "Start Hand"
   - Используйте кнопки для действий (Fold, Check, Call, Raise, All-in)

## 💡 Полезные команды

```bash
# Установка зависимостей отдельно
npm --workspace=backend install
npm --workspace=frontend install
npm --workspace=contracts install

# Запуск всего одной командой
npm run dev

# Запуск только фронтенда
npm run dev-frontend

# Запуск только бекенда
npm run dev-backend

# Тестирование контрактов
npm run test

# Компиляция контрактов
npm --workspace=contracts run compile
```

## 🔗 Интеграция с Web3 кошельком

Для production интеграции с MetaMask:

1. Установите Web3React:
```bash
npm --workspace=frontend install @web3-react/core @web3-react/injected-connector
```

2. Добавьте Web3Provider в frontend
3. Используйте контрактный адрес из `contracts/deployment.json`

## 🐛 Решение проблем

### "Cannot connect to WebSocket"
- Убедитесь, что бекенд запущен на порту 8081
- Проверьте .env переменные в фронтенде

### "Contract not deployed"
- Запустите `npm run deploy` в папке contracts
- Скопируйте адрес контракта в backend/.env

### "MetaMask не видит сеть"
- Добавьте Localhost 8545 сеть в MetaMask:
  - Network Name: Hardhat
  - RPC URL: http://localhost:8545
  - Chain ID: 31337

## 📊 Структура проекта

```
poker-game/
├── backend/          - Express server + WebSocket
├── frontend/         - Next.js UI
├── contracts/        - Solidity смарт-контракты
├── docker-compose.yml
└── package.json      - Монолит workspace
```

## ✅ Проверка работоспособности

После запуска всех сервисов:

1. **Фронтенд**: http://localhost:3000 - должна загружаться главная страница
2. **API**: http://localhost:8080/api/health - должна быть `{"status": "ok"}`
3. **WebSocket**: Подключение должно автоматически работать при присоединении к столу

## 🎁 Тестовые счета Hardhat

Используйте эти счета для тестирования с предзагруженным ETH:

```
Account #0: 0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266
Private Key: 0xac0974bec39a17e36ba4a6b4d238ff944bacb476c10d2754bab4eddc4f7f2738
...
(всего 20 счетов доступно)
```

## 🚀 Готово к запуску!

Теперь у вас есть полностью рабочая децентрализованная игра в покер! 🎰

Для вопросов обратитесь к README.md в корне проекта.
