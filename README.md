# 🎰 Poker Game - Decentralized Ethereum Integration

Полнофункциональная игра в покер с фронтенд, бекенд и смарт-контрактом для Ethereum.

## 🌟 Особенности

- **Real-time Gameplay**: WebSocket для синхронизации игры в реальном времени
- **Multiple Table Sizes**: Поддержка столов для 1, 6 и 9 игроков
- **Ethereum Integration**: Смарт-контракт для управления ставками и выплатами
- **Complete Poker Logic**: Полная логика покера (prefop, flop, turn, river, showdown)
- **Lobby System**: Система лобби для создания и присоединения к столам
- **Player Accounts**: Система учета с депозитами и выводами ETH
- **Hand History**: Ведение истории рук и статистики игроков

## 📋 Требования

- Node.js v16+
- npm или yarn
- Hardhat (для контрактов)
- MetaMask или другой Web3 кошелек

## 🚀 Установка и запуск

### 1. Клонируйте репозиторий
```bash
cd poker-game
```

### 2. Бекенд

```bash
cd backend
npm install
cp .env.example .env

# Запустите бекенд сервер
npm start
```

Бекенд будет запущен на `http://localhost:8080` и WebSocket на `ws://localhost:8081`

### 3. Смарт-контракт

```bash
cd contracts
npm install

# Скомпилируйте контракт
npm run compile

# Запустите тесты
npm test

# Разверните на локальной сети (требует Hardhat node)
npm run deploy
```

### 4. Фронтенд

```bash
cd frontend
npm install

# Запустите фронтенд (dev mode)
npm run dev
```

Фронтенд будет доступен на `http://localhost:3000`

## 🎮 Игровой процесс

### Создание лобби
1. Перейдите на главную страницу
2. Нажмите "Create New Lobby"
3. Заполните параметры:
   - Название лобби
   - Размер стола (1, 6, или 9 игроков)
   - Минимальная и максимальная ставки

### Присоединение к игре
1. Выберите лобби из списка
2. Создайте новый стол или присоединитесь к существующему
3. Введите ваше имя и начальный стек
4. Начните играть!

### Действия в игре
- **Fold**: Сбросить карты
- **Check**: Уравнять без ставки
- **Call**: Уравнять текущую ставку
- **Raise**: Увеличить ставку
- **All-in**: Поставить все фишки

## 💰 Ethereum интеграция

### Процесс ставок

1. **Депозит**: Игроки депозитят ETH в смарт-контракт через функцию `depositFunds()`
2. **Присоединение**: При присоединении к столу ставка вычитается из баланса
3. **Выплата**: Победитель получает выигрыш минус комиссия платформы (0.5%)

### Смарт-контракт функции

```solidity
// Депозит ETH
function depositFunds() external payable

// Вывод ETH
function withdrawFunds(uint256 amount) external

// Создание стола
function createTable(string memory tableId, uint8 maxPlayers, uint256 minBet, uint256 maxBet) external

// Присоединение к столу
function joinTable(string memory tableId, uint256 stake) external

// Размещение ставки
function placeBet(string memory tableId, uint256 amount) external

// Завершение игры и выплата
function endGame(string memory tableId, address winner, uint256 payout) external
```

## 🏗️ Архитектура

```
poker-game/
├── backend/                 # Express + WebSocket сервер
│   ├── server.js           # Основной сервер
│   ├── package.json
│   └── .env.example
├── frontend/               # Next.js приложение
│   ├── pages/
│   │   ├── index.tsx       # Главная страница (лобби)
│   │   ├── create-lobby.tsx # Создание лобби
│   │   ├── lobby/[id].tsx  # Страница лобби
│   │   └── table/[id].tsx  # Игровой стол
│   ├── lib/
│   │   ├── api.ts          # API клиент
│   │   ├── websocket.ts    # WebSocket менеджер
│   │   └── gameStore.ts    # Zustand store
│   └── package.json
└── contracts/              # Solidity смарт-контракты
    ├── PokerGame.sol       # Основной контракт
    ├── hardhat.config.js
    ├── scripts/
    │   └── deploy.js
    ├── test/
    │   └── PokerGame.test.js
    └── package.json
```

## 🔧 Конфигурация

### Backend (.env)
```
PORT=8080
WS_PORT=8081
NODE_ENV=development
ETHEREUM_RPC_URL=http://localhost:8545
CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000
PRIVATE_KEY=your_private_key_here
```

### Frontend (.env.local)
```
NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_WS_URL=ws://localhost:8081
```

## 📊 API Endpoints

### REST API
- `GET /api/lobbies` - Получить список лобби
- `POST /api/lobbies` - Создать новое лобби
- `GET /api/tables/:tableId` - Получить информацию о столе
- `POST /api/tables` - Создать новый стол
- `GET /api/health` - Проверка здоровья сервера

### WebSocket Events
- `connected` - Подключение к серверу
- `joined-table` - Присоединение к столу
- `table-update` - Обновление состояния стола
- `error` - Ошибка

## 🧪 Тестирование

### Запуск тестов контракта
```bash
cd contracts
npm test
```

### Локальное тестирование Ethereum

Запустите Hardhat node:
```bash
npx hardhat node
```

Это создаст локальную сеть на `http://localhost:8545`

## 🔐 Безопасность

- Все ставки хранятся в смарт-контракте
- Используется OpenZeppelin для best practices
- Валидация входных данных на уровне контракта
- Комиссия платформы предотвращает атаки

## 📈 Масштабирование

### Для production:
1. Разверните смарт-контракт на Ethereum основной сети
2. Используйте production базу данных для state
3. Добавьте аутентификацию через Web3 кошельки
4. Реализуйте улучшенную обработку конфликтов RNG
5. Добавьте мониторинг и логирование
6. Используйте CDN для фронтенда
7. Масштабируйте бекенд с load balancers

## 🐛 Известные ограничения

- RNG для карт сейчас псевдослучайный (для production нужен Chainlink VRF)
- Максимум одного хенда одновременно на столе
- Нет поддержки турниров (только cash games)
- Базовая валидация действий (можно улучшить)

## 🚀 Будущие улучшения

- [ ] Chainlink VRF для честной RNG
- [ ] Турниры и SNG игры
- [ ] Мобильное приложение
- [ ] Live spectating
- [ ] Чат в лобби
- [ ] Leaderboard
- [ ] Различные покер варианты (Omaha, 7-Card Stud)
- [ ] Рейтинговая система
- [ ] Интеграция с различными блокчейнами

## 📝 Лицензия

MIT

## 💬 Поддержка

Если у вас есть вопросы или проблемы, создайте issue в репозитории.

## 👨‍💻 Разработчики

Создано как полнофункциональная система для децентрализованного покера на Ethereum.

---

**Версия**: 1.0.0  
**Последнее обновление**: 2026
